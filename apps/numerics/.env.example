# Massive market data API (https://api.massive.com)
MASSIVE_API_KEY=

# Local parquet cache directory (relative paths resolve against this file's dir).
# Defaults to .cache/massive when unset.
MASSIVE_CACHE_DIR=.cache/massive
